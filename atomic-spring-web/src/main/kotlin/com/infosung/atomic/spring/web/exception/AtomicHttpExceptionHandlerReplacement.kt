package com.infosung.atomic.spring.web.exception

/**
 * Marker for a host-provided [BaseExceptionHandler] that intentionally replaces the built-in scoped
 * [AtomicHttpExceptionHandler].
 */
interface AtomicHttpExceptionHandlerReplacement
